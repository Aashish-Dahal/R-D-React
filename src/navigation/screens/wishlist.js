import React from 'react';

import {Text, StyleSheet} from 'react-native';
export default function Wishlist() {
  return <Text style={styles.textStyle}>Wishlist Screen</Text>;
}
const styles = StyleSheet.create({
  textStyle: {
    color: 'black',
    fontSize: 20,
  },
});
